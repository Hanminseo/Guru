<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swu_2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SWU2">

        <activity android:name=".Website" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".cal_test" />
        <activity android:name=".Join" />
        <activity android:name=".Gonggi" />

        <activity android:name=".Password" />
        <activity android:name=".SwuInfo" />
        <activity android:name=".AccountInfo" />
        <activity android:name=".StartLogo">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>